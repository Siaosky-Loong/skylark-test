# 快速使用指南

## 🚀 快速开始

### 方式一：在Web页面中输入API密钥（推荐）
1. 启动应用：`./start.sh` 或 `source venv/bin/activate && python app.py`
2. 访问：http://localhost:8000
3. 在页面中直接输入API密钥和端点ID

### 方式二：使用环境变量
```bash
# 复制环境变量模板
cp .env.example .env

# 编辑.env文件，设置您的API密钥
# ARK_API_KEY=your_actual_api_key_here
```

## 📝 使用步骤

1. **配置API密钥**（推荐在Web页面输入）
   - 在页面的"API密钥"输入框中输入您的BytePlus ModelArk API密钥
   - 密钥将安全存储在浏览器本地，不会发送到服务器存储

2. **配置模型参数**
   - **模型端点ID**：输入您在BytePlus控制台创建的端点ID
   - **模型ID**（可选）：如果需要直接指定模型，可以输入模型ID
     - 如果填写了模型ID，将使用模型ID作为API调用的model参数
     - 如果未填写模型ID，将使用端点ID作为model参数

3. **设置System消息**（可选）
   - 在"System消息"文本框中输入系统提示词
   - 用于设定AI的角色和行为规范

4. **输入User消息**
   - 在"User消息"文本框中输入您的问题或对话内容

5. **发送消息**
   - 点击"发送消息"按钮或使用快捷键 Ctrl+Enter (Windows/Linux) 或 Cmd+Enter (Mac)

6. **查看响应**
   - AI的回复将显示在响应区域
   - 页面底部会显示Token使用统计信息

## 🔐 安全特性

- ✅ API密钥使用密码输入框，不会明文显示
- ✅ API密钥安全存储在浏览器本地存储中
- ✅ 服务器日志不会记录API密钥信息
- ✅ 支持清除本地存储的敏感信息

## 🔧 配置说明

### API参数 (已按要求配置)
- **temperature**: 1.0
- **top_p**: 0.7
- **logit_bias**: {"861": -100, "854": -100, "135": -100, "136": -100}

### 获取API密钥和端点ID
1. 访问 [BytePlus控制台](https://console.byteplus.com/)
2. 创建或选择ModelArk项目
3. 获取API密钥 (ARK_API_KEY)
4. 创建模型端点并获取端点ID

## 💡 功能特性

- ✅ **Web页面输入API密钥** - 无需编辑配置文件
- ✅ 支持System和User消息
- ✅ 实时API调用
- ✅ 美观的Web界面
- ✅ 自动保存配置到本地存储
- ✅ Token使用统计
- ✅ 完善的错误处理
- ✅ 响应式设计

## 🛠️ 故障排除

### 常见问题
1. **API密钥错误**: 检查输入的API密钥是否正确
2. **端点ID错误**: 确认端点ID正确且已激活
3. **网络连接**: 检查网络连接和防火墙设置
4. **端口被占用**: 修改app.py中的端口号

### 快捷键
- **Ctrl+Enter**: 在用户输入框中快速发送消息

### 清除数据
- 点击"清空输入"按钮可选择性清除API密钥和配置信息